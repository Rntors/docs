---
title: "RNTOR MCP server"
sidebarTitle: "Overview"
description: "Connect Claude, ChatGPT, and other MCP clients to your RNTOR network with 35 secure, read-only tools."
---

RNTOR MCP connects AI assistants such as Claude and ChatGPT to your live workspace operations through the open [Model Context Protocol](https://modelcontextprotocol.io).

Ask operational questions in plain language instead of switching between dashboards or building one-off reports. RNTOR verifies the signed-in user, limits access to the selected network and environment, and returns only data that user is allowed to see.

<CardGroup cols={2}>
  <Card title="Connect with ChatGPT" icon="message" href="/mcp-server/chatgpt">
    Install or connect RNTOR in ChatGPT, authorize a network, and start asking questions.
  </Card>

  <Card title="Connect with Claude" icon="sparkles" href="/mcp-server/claude">
    Install the published RNTOR community connector from the Claude Directory.
  </Card>
</CardGroup>

## What you can do

RNTOR exposes **35 canonical read-only tools** across the operational areas below:

- **Networks and locations** — view the networks and locations you are authorized to access.
- **Members and companies** — find customer records and account status while protecting restricted contact data.
- **Plans and subscriptions** — compare membership and resource plans, active subscriptions, and renewal dates.
- **Resources and bookings** — explore rooms, desks, offices, parking spaces, equipment, and bookings.
- **Invoices and contracts** — review invoice status and balances, contracts, and contract templates without exposing private files.
- **Shop and orders** — inspect categories, products, and orders across locations.
- **Benefits and applications** — review member benefits, categories, and submitted applications.
- **Guides** — retrieve published member guides and operational information.

<Note>
  RNTOR MCP is read-only. It cannot create, edit, delete, approve, charge, refund, sign, or trigger workflows in your RNTOR account.
</Note>

## Connect RNTOR MCP

Use the canonical server URL for every new connection:

```text
https://mcp.rntor.com/mcp
```

There are no API keys to copy. RNTOR uses OAuth: sign in, select the network you want to authorize, review the read-only permissions, and choose **Allow**.

<Tip>
  If you can access multiple RNTOR networks, each connection is restricted to the network selected during authorization. Disconnect and reconnect to switch the authorized network.
</Tip>

## Supported tools

Each tool has a structured output schema and truthful read-only annotations. List tools use bounded pagination and scoped cursors; date-based tools validate and pin their requested time window across pages.

### Identity, networks, and locations

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getCurrentUser` | The signed-in identity used by the connection | “Which RNTOR account is connected?” |
| `getNetworks` | Networks the authorized user can access | “Which workspace networks can I access?” |
| `getNetwork` | Details for one authorized network | “Show the key details for our network.” |
| `getLocations` | Authorized, non-internal locations | “List our locations and their addresses.” |
| `getLocation` | Details for one authorized location | “Show the contact details for our Downtown location.” |

### Customers

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getCustomers` | Members and companies, filterable by location, type, status, and search | “Which companies are active at Downtown?” |
| `getCustomer` | One authorized member or company record | “Show this member’s account and subscription status.” |

### Plans and subscriptions

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getPlans` | Membership and resource plans with derived plan type | “Compare our active membership plans.” |
| `getPlan` | Details for one plan | “What is included in our Dedicated Desk plan?” |
| `getSubscriptions` | Subscriptions filtered by customer, status, plan, location, or date | “Which subscriptions renew this month?” |
| `getSubscription` | Details for one authorized subscription | “When does this subscription renew?” |

### Resources and bookings

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getResources` | Authorized resources by location and type | “List meeting rooms and their capacities.” |
| `getResource` | Details for one resource | “What is the capacity of the Boardroom?” |
| `getBookings` | Bookings by resource, customer, status, and bounded time range | “What is booked in the Boardroom this week?” |
| `getBooking` | Details for one authorized booking | “Who booked the Boardroom at 2 PM?” |

### Invoices, contracts, and templates

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getInvoices` | Invoice records, status, totals, and derived balances | “List unpaid invoices due this week.” |
| `getInvoice` | Details and line items for one invoice | “What does this invoice cover and what remains due?” |
| `getContracts` | Contracts filtered by customer, location, status, or date | “Which contracts are awaiting signature?” |
| `getContract` | Details for one authorized contract | “What is the status and renewal date of this contract?” |
| `getTemplates` | Staff-authorized contract templates | “Which contract templates are available?” |
| `getTemplate` | Metadata for one contract template | “Show the details of our membership agreement template.” |

<Note>
  Private document links, storage paths, e-signature details, provider identifiers, and signed URLs are not returned through MCP.
</Note>

### Shop and orders

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getShopProducts` | Products by location, category, and status | “List active shop products and prices.” |
| `getShopProduct` | Details and options for one product | “What options are available for this product?” |
| `getShopCategories` | Shop categories by location | “List our shop categories.” |
| `getShopCategory` | Details for one shop category | “Which category is this product in?” |
| `getOrders` | Orders by customer, product, category, status, or time range | “What did members order this week?” |
| `getOrder` | Details and items for one order | “What was included in this order?” |

### Benefits and applications

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getBenefits` | Published benefits by location, category, and status | “What member benefits are currently available?” |
| `getBenefit` | Details and terms for one benefit | “What are the terms of this benefit?” |
| `getBenefitCategories` | Benefit categories | “List our benefit categories.” |
| `getBenefitCategory` | Details for one benefit category | “Show this benefit category.” |
| `getBenefitApplications` | Applications by benefit, customer, status, and time range | “Which benefit applications are pending?” |
| `getBenefitApplication` | Details for one authorized application | “Show the status of this benefit application.” |

### Guides

| Tool | What it returns | Example prompt |
| --- | --- | --- |
| `getGuides` | Published guides available in a location | “List the guides published for members.” |
| `getGuide` | Capped content for one published guide | “Open our Wi-Fi guide.” |

## Security and privacy

- **OAuth authentication** — every connection is tied to a validated RNTOR user.
- **Explicit network authorization** — the OAuth consent screen requires a network selection, and the binding is activated only after authorization succeeds.
- **RLS-backed access** — RNTOR database policies enforce tenant and user permissions.
- **Live/test separation** — tools default to the live environment and never mix live and test records.
- **Internal-location protection** — internal operational locations are excluded.
- **Data minimization** — sensitive provider, payment, signing, storage, and internal-only fields are omitted.
- **Safe errors** — inaccessible and nonexistent identifiers return indistinguishable responses.
- **Read-only execution** — every tool is annotated as read-only and performs no business-data writes.

## FAQs

### Can RNTOR MCP change my data?

No. The current 35-tool surface only retrieves data.

### Which network can the assistant access?

Only the network selected on the RNTOR OAuth consent screen, and only within the signed-in user’s RNTOR permissions.

### Can I connect a test network?

Yes. The environment can be selected explicitly where supported, but live and test data are isolated.

### What happens if I reach a rate limit?

Reduce parallel requests, allow a short pause, and retry. RNTOR returns a standard `429` response when temporary limits apply.

### How do I end access?

Disconnect RNTOR in your AI assistant. You can reconnect later and authorize a network again.

For help, contact [support@rntor.com](mailto:support@rntor.com).